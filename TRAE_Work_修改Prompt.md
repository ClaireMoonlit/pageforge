# 给 TRAE Work 的修改 Prompt

> 用法：在原对话里继续发（保留上下文），或新开 Auto 模式对话粘贴。把下面 ``` 内的内容粘贴发送。

## 修改 1：新增「产品形态预览」章节（最重要）

```
请在现有创意提案 HTML 中新增一个章节「产品形态预览」，插入位置：在"04 核心闭环"之后、"05 价值与意义"之前。原 05/06/07 章节序号顺延为 06/07/08，顶部导航的锚点和文字同步更新。

这一节用纯 HTML+CSS 画一个"造页工坊"编辑器界面的高保真 mockup（不依赖任何外部资源、不用图片、全部用 div+CSS 绘制），让评委一眼看懂产品长什么样。

mockup 布局（外层一个带圆角和边框的 .mock 容器，模拟应用窗口，顶部带红黄绿三个小圆点模拟窗口控制按钮）：

1. 顶部工具栏 .mock-toolbar：
   - 左侧：logo「造页工坊 PageForge」（小字号，品牌色）
   - 中间：一组工具按钮（撤销 ↶、重做 ↷、预览 👁、导出 ⬇），用小圆角方块按钮表示
   - 右侧：设备切换（🖥 📱 📱横屏 三个小图标）+ 一个圆形头像占位

2. 主体三栏 .mock-body（grid: 左侧 200px / 中间 1fr / 右侧 240px，移动端折叠为纵向）：
   - 左侧组件库 .mock-left：标题「组件库」，下方列出 6 个可拖拽组件项（带小图标和文字）：标题 H1、图片 🖼、按钮 ▢、卡片 ▦、表单 ✎、容器 ▢），每项 hover 高亮
   - 中间画布 .mock-canvas：浅色画布背景，里面放一个示例落地页（一个大标题"新品发布" + 副标题一行 + 一个品牌色按钮"立即了解" + 下方两列卡片各一张带图标和文字）。在示例页面某个元素上画一个蓝色虚线选中框 + 四角小拖拽点；在两个元素之间画一条粉色细虚线表示"智能吸附对齐线"；画布右下角放一个浮动小标签"规则推断：同行 → flex-row"
   - 右侧属性面板 .mock-right：顶部三个标签页（样式 / 布局 / AI助手，当前选中"样式"），下方显示选中元素的属性表单：宽度输入框、内边距输入框、背景色色块、圆角滑块、字号输入框等（用静态 div 模拟表单控件，不要真功能）

3. mockup 下方加一行说明文字："▲ 造页工坊编辑器界面概念图——左侧拖组件、中间自由摆放（智能吸附+规则推断）、右侧调样式，一键导出干净 HTML"

视觉要求：
- 与整页深色紫蓝渐变风格一致，但 mockup 内部用稍浅的卡片背景以区分
- 选中框用 #6366f1 虚线，对齐线用 #ec4899 虚线
- 所有元素静态呈现即可，不需要真交互，但要看起来像真实软件截图
- 移动端（max-width:720px）三栏折叠为纵向：工具栏→组件库横向→画布→属性面板

同时：把顶部导航里"03 用户痛点"的文字改为"03 目标用户与痛点"，与该章节实际标题统一。
```

## 修改 2：报名帖标题引号（可选，发帖前手动改即可）

报名帖标题原文：
【学习工作赛道】造页工坊 —— 像做 PPT 一样"造"网页的智能可视化 HTML 编辑器

建议改为（去掉"造"的引号，避免标题嵌套引号渲染问题）：
【学习工作赛道】造页工坊 —— 像做 PPT 一样造网页的智能可视化 HTML 编辑器

> 这条不用给 work，发帖时手动把"造"两边的引号删掉即可。

## 修改 3：报名帖第 3 部分微调（可选）

若想让 work 帮微调报名帖第 3 部分（把商业价值从社会价值段落里独立出来，更贴合官方"选 1-2 个角度"要求），可发：

```
请把报名帖正文第 3 部分"价值与意义"调整为两个清晰角度（效率提升 + 社会价值），把目前混在社会价值段落末尾的"商业上则可通过模板市场分佣 + 可选 AI Pro 订阅 + 企业版协作 + 一键托管增值实现多路径变现"这句单独拎出来作为第三段"商业价值"的简短补充，但明确标注商业价值为次要角度。整体保持 150 字以内，节奏更紧凑。其余三部分不动。
```
